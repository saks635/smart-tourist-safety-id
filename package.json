{
  "name": "smart-tourist-safety-id",
  "version": "1.0.0",
  "description": "Blockchain-based tourist identification system with geo-fencing safety alerts",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network sepolia",
    "node": "npx hardhat node",
    "test": "npx hardhat test"
  },
  "keywords": ["blockchain", "ethereum", "tourist", "safety", "geofencing", "web3"],
  "author": "Smart Tourist Safety Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "web3": "^4.0.0"
  }
}
